git clone https://github.com/nyu-mll/jiant.git
cd jiant
# pip install -r requirements.txt : Avoided this because it seems to just be torch

# Add the following to your .bash_rc or .bash_profile 
export PYTHONPATH=./jiant:$PYTHONPATH

# Turns out there were more linked installs so better to start a new virtual env
# COmbined two req files into req-joined.txt . This one works
# Results seem a bit off though, upgrade to new transformers ver?
#       Better when swapping to google-bert/bert-base-uncased. Actually it seems results reported in paper are for bert-large-cased

python3 -m pip install nlpaug gensim
pip install scipy==1.10.1
cp ../bert_exp/glove-embeddings/ ./ -r
cp ../low_rank_modules/ ./ -r
pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
pip3 install dill
pip3 install datasets
